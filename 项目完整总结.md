# 🎃 南瓜子外观质量分类 — 项目完整总结

> **项目时间**：2026 年 8 月
> **开发者**：兰天宇（lty23311111）
> **关联项目**：[西瓜子外观质量分类](..\图像检测(西瓜子)\项目完整总结.md)（同配方模板，本报告对比基线）

---

## 一、项目概述

用海康工业相机逐颗拍摄南瓜子，通过深度学习自动将南瓜子分为 **3 个外观质量等级**。项目基于西瓜子项目验证过的完整流水线（标注 → 训练 → 交叉验证 → GUI 推理 → 现场验收）复刻，并新增了**预处理缓存优化**（将 11GB 原始 BMP 转为 565MB 缓存 PNG，训练提速约 5 倍且质量经实证无损失）。

### 3 个等级（比西瓜子少一级）

| 等级 | 含义 | 数量 |
|------|------|------|
| 一级 · 完好 | 无瑕疵 | 220 张 |
| 二级 · 轻微瑕疵 | 少量/轻微缺陷 | 200 张 |
| 三级 · 明显瑕疵 | 明显缺陷 | 180 张 |

> **为何只做 3 类**：南瓜子基本都较完好，用户不打算手动掰断去制造「严重残缺」类（西瓜子的第四级），所以把尾部分并成「明显瑕疵」一类。

---

## 二、最终成绩

| 指标 | 数值 |
|------|------|
| 数据集 | 600 张 BMP（3072×2048，约 18MB/张，合计约 11GB） |
| 拍摄条件 | 海康相机 + 固定光源，南瓜子表面平滑，**略微偏暗**拍摄即可 |
| 类别分布 | 一级 220 · 二级 200 · 三级 180 |
| **单模型最佳（验证）** | **92.50%**（留出验证准确率） |
| **全量准确率** | **97.17%**（仅拟合检查） |
| **K 折均值（诚实水平）** | **90.67% ± 2.86%**（留出验证） |
| 各类 F1（K 折） | 一级 94.77% ± 2.71% · **二级 85.83% ± 4.41%** · 三级 90.97% ± 2.04% |
| 推理 | 8 模型软投票（best + 5 折 + Top-3） |
| 设备 / 环境 | RTX 4060 Laptop (8.6GB) + CUDA 12.6 + PyTorch 2.12.1 + `C:\Users\LTY\anaconda3\python.exe` |

### 质检安全性（最终模型验证集）

| 方向 | 情况 | 含义 |
|------|------|------|
| 一级 precision | **1.000**（44/44 全对） | 没有坏籽被放行成「完好」——最关键的安全性质 |
| 二级 → 三级 | 6/40 被判严 | 模型在中间带偏严（宁可多挑出），品控上可接受 |
| 三级 → 二级 | 1/36 漏检 | 边界噪声，非系统性缺陷 |

---

## 三、技术方案

### 配方（与西瓜子完全一致，即插即用）

| 组件 | 方案 |
|------|------|
| 模型 | ResNet-50（ImageNet1K_V2 预训练） |
| 分类头 | Dropout(0.4) + Linear(2048, 3) |
| 数据增强 | RandomResizedCrop(0.7~1.0) + 水平/垂直翻转 + 旋转30° + ColorJitter + RandomGrayscale + RandomErasing |
| 类别平衡 | WeightedRandomSampler 均衡采样 |
| 损失函数 | 普通交叉熵 |
| 优化器 | AdamW (lr=0.001, weight_decay=1e-3) |
| 调度器 | CosineAnnealingLR (T_max=60) |
| Batch / Epoch | 32 / 60，输入 224×224 |

### 关键决策

1. **预处理缓存优化（本项目新增）**：18MB BMP 读盘解码太慢，训练一 epoch 约 20s。把原图按最长边 1024 无损缩成 PNG 缓存（`data/cache_1024`，565MB），训练提速约 5 倍。**用实验验证质量中性**：同一模型对全分辨率 vs 1024 缓存预测，599/600 一致（99.83%），仅 1 张边界图翻转 → 判定 [OK] 可放心使用。512 缓存（99.50%）不合格被弃。
2. **K 折用留出验证准确率排名**：全量准确率含「背题分」，模型排名只看没见过的留出数据（与西瓜子审查结论一致）。
3. **软投票部署**：gui / predict / demo 用 8 模型概率平均（软投票），比硬投票稳——600 张里仅 3 张与硬投票不同，且软投票对人标一致率 98.83% 略高于硬投票 98.67%。
4. **复查工具用硬投票展示票数**：review_3class.py 用 `Counter` 统计每类票数（"一级 5 票 二级 3 票"）给人工看，这是其展示职责决定的，与部署工具的软投票不冲突。

---

## 四、完整演进历程

### 标注迭代主线（与西瓜子同思路：一致性 > 调参）

| 阶段 | 留出验证 | 全量 | 说明 |
|------|---------|------|------|
| 首标训练 | **83.3%** | 92.7% | 600 张首标，一二级边界矛盾多 |
| 复查第 1 轮 | **85.0%** | 92.0% | 模型辅助找出 13 张 二级→一级 |
| 复查第 2 轮 | **92.5%** | 97.0% | 再找出 13 张 二级→一级（219/201/180） |
| 5 折交叉验证 | **90.67% ± 2.86%** | 96.60% | 诚实水平：单次 92.5% 含划分运气 |
| 定稿前定夺 | — | — | 5 张人机分歧图：1 张改标（二级→一级），其余保留 |
| 最终训练 | **92.50%** | 97.17% | 标注定稿（220/200/180），Top-3 归档 |

### 数字里程碑

```
83.3% ── 首标训练
85.0% ── 复查第 1 轮后（13 张 二级→一级）
92.5% ── 复查第 2 轮后（13 张 二级→一级）
90.7% ± 2.9% ── 5 折交叉验证（真实泛化水平）
92.5% / 97.2% ── 最终训练（标注定稿）
```

### 关键洞察

1. **中间类（二级）最弱是本质**：F1 85.8%，比两侧低 5~9 个点。一、三级模型 100% 无分歧，全部残余错误都压在中边界的「轻微瑕疵」上——这不是调参能救的，是分类标准本身的模糊带。
2. **两轮复查价值可量化**：83.3% → 92.5% 全靠标注修正，与西瓜子「13 张边界样本修正让 K 折从 90% 提到 96%」完全同构。
3. **单次验证分有运气**：单次 92.5% 是某一份随机划分，K 折均值 90.7% ± 2.9%（折间差异 86.7%~95.0%）才是诚实数字。
4. **不要追着模型改标注**：复查到 K 折数字干净后即停，再往下追会让人标迁就模型，验证准确率失真。

---

## 五、工具链清单

| 工具 | 用途 |
|------|------|
| `annotate_3class.py` | 标注 GUI：按键 1/2/3 分拣到类别文件夹，支持断点续标 |
| `train_3class.py` | 日常训练，自动维护 Top-3 最佳模型（验证准确率排名 + 文件移位对齐） |
| `train_kfold_3class.py` | 5 折交叉验证，只覆盖更好的折，写 `kfold_log.csv` |
| `review_3class.py` | 复查人机不一致样本：8 模型投票 + 票数展示 + 重标（先复制后删，防丢失） |
| `verify_cache.py` | 缓存质量验证：全分辨率 vs 缓存预测一致率，`CACHE_SIZE` 可调 |
| `predict.py` | 命令行推理（单张/文件夹，打印投票明细） |
| `gui.py` | 图形界面：拖拽/单张/批量 + 概率条 + 导出 CSV + 双击预览 |
| `demo.py` | 现场验收：逐张预测 + 人工按键标注 + 实时一致率 + 导出验收报告 |

---

## 六、踩坑经验

1. **缓存分辨率要验证，不能拍脑袋**：512 缓存 3 张翻转（99.50%），1024 只有 1 张（99.83%）——分辨率不是越高越好，但要够，用一致率实验定，而不是猜。
2. **验证准确率 ≠ 最终成绩**：单次 92.5% 好听，K 折 90.7% ± 2.9% 才是真实水平；折间标准差 2.9% 说明划分运气真实存在。
3. **中间类永远是瓶颈**：边界类 F1 天然低于两侧，除非重新定义分级标准，否则这是上限，别反复调参追。
4. **训练日志/CSV 文件别开着跑训练**：VS Code/Excel 锁定会导致 PermissionError（脚本已做降级处理，但关掉文件更省事）。
5. **GBK 控制台编码坑**：终端输出中文/emoji 会乱码或崩溃，脚本内用了 [OK]/[WARN] 等纯文本标记规避。
6. **人机复查最后一步要克制**：5 张分歧图定夺时，只有 1 张确属标错；其余是主观边界，保留即止——把标准定义清楚比把数字刷高更重要。

---

## 七、与西瓜子项目的差异

| 维度 | 西瓜子 | 南瓜子 |
|------|--------|--------|
| 分类数 | 4（含严重残缺） | 3（明显瑕疵合并） |
| 数据量 | 372 张 | 600 张 |
| 拍摄曝光 | 过曝（表面凹凸不平） | 略暗即可（表面平滑） |
| 缓存优化 | 无 | 新增 cache_1024（提速 ~5 倍，实证质量中性） |
| K 折均值 | 96.18% ± 0.87% | 90.67% ± 2.86% |
| 代码复用率 | — | 约 90% 模板直接复用，仅改分类数/路径/类别名 |

> **南瓜子准确率低于西瓜子**：主要原因是二级（轻微瑕疵）这类中间带更难，且南瓜子瑕疵更轻微、与完好边界更模糊；不是流程问题——一、三级模型已非常干净（100% 无分歧）。

---

## 八、最终交付物

```
图像检测（南瓜子）/
├── data/
│   ├── raw/                  # 600 张原始 BMP（约 11GB）
│   ├── annotated_3class/{1,2,3}/   # 人工标注（220/200/180）
│   └── cache_1024/{1,2,3}/         # 预处理缓存 PNG（565MB，训练用）
├── models/
│   ├── best_model_3class.pth       # 榜首模型（改标前训练）
│   ├── best_model_3class_2.pth     # 最终模型（改标后训练，val 0.9250）
│   ├── best_model_3class_3.pth
│   ├── fold_1.pth ~ fold_5.pth     # K 折模型（投票用）
│   └── _best_record / _kfold_record（记录 json）
├── *.py（8 个工具脚本，见第五节）
├── training_log.csv         # 4 次训练记录
└── kfold_log.csv            # 5 折交叉验证记录
```

### 部署说明

- **推理入口**：`python gui.py`（图形界面）/ `python predict.py <图或文件夹>`（命令行）/ `python demo.py`（现场验收）
- **投票构成**：8 模型（best + _2 + _3 + 5 折）软投票（概率平均）
- **若要重新训练**：直接跑 `train_3class.py`（数据源已切 cache_1024）；想用全分辨率原始图，把脚本里 `ANNOTATED` 改回 `"annotated_3class"` 即可

---

## 九、简历可用版本

> **南瓜子外观质量分类系统** | 独立开发 | 2026.08
>
> 基于 PyTorch + ResNet-50 搭建工业视觉自动分级系统，将南瓜子按外观质量分为 3 级。独立完成 600 张数据标注、模型训练、5 折交叉验证与 GUI 部署全流程。通过两轮系统性标注修正，将模型验证准确率从 83.3% 提升至 92.5%（K 折真实泛化水平 90.7% ± 2.9%）；一级（完好）类别零误放行，质检安全方向正确。设计了预处理缓存优化（11GB 原始图 → 565MB 缓存，训练提速 5 倍），并用预测一致率实验（99.83%）实证其质量中性。核心经验：小样本 + 模糊边界场景下，标注一致性远比算法调参重要。

---

## 十、项目数字速览

```
📸 拍摄 600 张   🏷 标注 3 级   🧠 训练 4 次
📊 交叉验证 5 折   🎯 单模型 92.5%   🎲 K折 90.7% ± 2.9%
⚡ 缓存提速 ~5×   🔍 缓存质量 99.83% 一致   🛠 工具 8 个   🤖 投票模型 8 个
```
